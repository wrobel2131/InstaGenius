spring:
  application:
    name: product-management-service
  config:
    import: optional:configserver:${CONFIG_SERVICE_URL:http://localhost:8888}

stripe:
  api-key: ${STRIPE_API_KEY} //TODO add this to external yaml