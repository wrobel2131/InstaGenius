spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${JWT_ISSUER_URI:http://localhost:8180/auth/realms/instagenius}
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:some-api-key}
      chat:
        enabled: true
      image:
        enabled: true
server:
  port: ${POST_GENERATION_SERVICE_PORT:8082}